Never replace existing TanStack routes with generated placeholders.
Reuse Product UI Library before creating new components.
Reuse Theme tokens; never hardcode colors.
No duplicated Tailwind classes when a reusable component exists.
No page-specific components if a generic one can be used.
No backend or API changes during frontend-only sprints.